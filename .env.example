# Cloudflare Web Analytics beacon token (public, not a secret).
# Get it from Cloudflare → Web Analytics → your site → JS snippet (the "token" value).
# Set this in Vercel → Settings → Environment Variables, then redeploy.
# When unset, no analytics script loads.
NEXT_PUBLIC_CF_BEACON_TOKEN=
